gym >= 0.26
numpy>=1.26.4
distrax>=0.1.2
ml_collections >= 0.1.0
tqdm >= 4.60.0
chex>=0.1.90
optax>=0.2
absl-py >= 0.12.0
scipy>=1.12
wandb >= 0.12.14
einops >= 0.6.1
imageio >= 2.31.1
moviepy >= 1.0.3
pre-commit == 3.3.3
overrides
cython < 3
patchelf
orbax-checkpoint>=0.4
flax>=0.10
brax
# JAX should be installed separately:
# pip install --upgrade "jax[cuda12]"
